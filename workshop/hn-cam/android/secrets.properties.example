# Copy to secrets.properties (git ignores it) for Wi-Fi mode.
# RELAY_HOST: the deployed relay's host name, e.g. hn-cam-relay.<you>.workers.dev
# RELAY_KEY:  the same value as the relay's KEY secret (relay/.dev.vars)
RELAY_HOST=
RELAY_KEY=
